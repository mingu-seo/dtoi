<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pdboard">

	<select id="count" parameterType="shop.pd_board.PdBoardVo" resultType="int">
		select count(*) as cnt from pd_board
	</select>
	
	<select id="selectList" resultType="shop.pd_board.PdBoardVo">
		select * from pd_board

		ORDER BY regdate desc 
		
	</select>
	
	<select id="selectOne" parameterType="shop.pd_board.PdBoardVo" resultType="shop.pd_board.PdBoardVo">
		select * from pd_board where re_no=${re_no}
	</select>
	
	
	
	
	<insert id="insert" parameterType="shop.pd_board.PdBoardVo">
		INSERT INTO pd_board (
		
			re_no,
			pd_no,
			cst_id,
			re_title,
			re_content,
			re_file,
			re_ori_file,
			re_regdate,
			re_score 
						
		) VALUES (
		
			#{re_no},
			#{pd_no},
			#{cst_id},
			#{re_title},
			#{re_content},
			#{re_file},
			#{re_ori_file},
			NOW(),
			#{re_score}
		)
	</insert>
	

	
	<delete id="delete" parameterType="shop.pd_board.PdBoardVo">
		DELETE FROM pd_board WHERE no = #{no}
	</delete>

	
</mapper>